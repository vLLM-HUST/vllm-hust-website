# Deprecated placeholder: main-dev is the only automation write target.

name: Back-Sync main → main-dev (disabled)

on:
  workflow_dispatch:

jobs:
  back-sync-disabled:
    name: Back-sync disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Back-sync is disabled. Automated content sync must write to main-dev only."
